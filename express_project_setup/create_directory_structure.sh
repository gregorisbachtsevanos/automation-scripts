#!/bin/bash

# Create directory structure in src
echo "Creating directory structure in src"
mkdir -p src/{config,constants,controllers,db,middleware,models,routes,utils}

echo "Directory structure created successfully."
